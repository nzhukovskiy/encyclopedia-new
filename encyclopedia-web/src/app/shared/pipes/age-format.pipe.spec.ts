import { AgeFormatPipe } from './age-format.pipe';

describe('AgeFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new AgeFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
