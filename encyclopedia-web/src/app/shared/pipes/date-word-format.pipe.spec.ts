import { DateWordFormatPipe } from './date-word-format.pipe';

describe('DateWordFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new DateWordFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
